# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ghost Rider' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ghost Rider

  pod "GradientView"

  target 'Ghost RiderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Ghost RiderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
